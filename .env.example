# 数据库（SQLite，首次运行执行 npm run db:push 自动创建）
DATABASE_URL=file:../db/custom.db

# ===== LLM 网关（可选，也可在应用内「设置」页配置，数据库配置优先级更高）=====
# 智谱开放平台: https://open.bigmodel.cn  (API Key 形如 xxx.xxx)
# LLM_API_KEY=
# LLM_BASE_URL=https://open.bigmodel.cn/api/paas/v4
# LLM_MODEL=glm-4-flash
