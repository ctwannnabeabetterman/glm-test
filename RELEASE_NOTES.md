# 发行说明 v1.2.4

> AI Network Lab —— 智能网络科研工作台
> 当前包版本：v1.2.4（对应 `package.json`）

**这一版把「检查更新」真正打通、换掉了默认图标，并给「多版本并发」装上了闸门。核心是修掉自动更新长期不生效的真因。**

---

## 一、自动更新为什么一直不生效（真因）

先给结论：**不是网络问题，也不是你机器的问题 —— 是发布侧的配置少写了一个键。**

`electron-builder.yml` 的 `publish` 段没有指定 `releaseType`。这个选项在 electron-builder 里**默认是 `draft`（草稿）**。
也就是说，CI 每次辛苦打完包，上传上去的都是一个**草稿 release**。而草稿在 GitHub 上：

- 不算「latest」——`https://github.com/<owner>/<repo>/releases/latest` 不再指向任何 tag（实测直接 302 到 releases 列表页）
- 匿名用户看不到

于是客户端这边：

1. electron-updater 先去取「最新 tag」，请求 `releases/latest` 并带 `Accept: application/json`
2. 跟着跳转走，GitHub 回了一个 **406**
3. 报错 `Cannot parse releases feed: ... HttpError: 406`
4. **而这句错误被 `console.warn` 悄悄吞掉了**

所以你在界面上看到的「检查更新毫无反应」，其实是**每次启动都在失败**，只是没人告诉你。

**已修**：显式写入 `releaseType: release`。

> ⚠️ **还需要你在 GitHub 上补一步**：历史上已经产生的那些 release 是草稿状态，需要手动改成正式发布；
> 或者重新打一个 tag 让 CI 覆盖。否则客户端依然找不到「latest」。

## 二、检查更新现在有界面了，而且不再静默失败

原来 `preload.js` 虽然暴露了检查更新的接口，但**前端一行都没调用过** —— 等于没有入口。现在：

- **设置页新增「软件更新」卡片**：显示当前版本、手动检查按钮，并如实告诉你结果
- **失败会翻译成人话**。同样一个失败，原来只给你一句 `HttpError: 406`，现在会说：
  「更新源暂时没有可用的正式发布（GitHub Releases）」——并附上「这是发布侧的问题，不是本机故障」
  - 出错分类：无正式发布 / 缺 `latest.yml` / 网络不可用 / 开发模式 / 更新组件缺失
- **有新版会三重提醒**：系统对话框 + 界面顶部常驻横幅（可忽略）+ 消息提示
  - 之所以不只靠对话框：用户点一次「稍后」对话框就消失了，等于提醒过了、但没记住。横幅只要没处理就一直挂着，并带「下载更新」按钮；下载完再给「立即重启并安装」

## 三、只能开一个客户端 —— 而且只允许存在一个版本

### 3.1 同一份安装的第二个实例（原有能力，这次堵上竞态）

单实例锁原本就在，实测也是生效的（双开时第二个实例会自动退出，关窗后进程全部归零、无残留）。
但里面有一个**退出竞态**值得堵上：原先拿不到锁只调 `app.quit()`，而 quit 是「请求退出」，
要等 `before-quit` 走完才真正结束，`app.whenReady()` 在这个窗口期仍可能被触发 ——
那样第二个实例会照样跑完启动流程、拉起第二份内部服务（多一批子进程 + 第二个 SQLite 句柄，
两个实例同时写同一个 db 文件是有风险的）。

现在改为 `app.exit(0)` 立即终止，并给启动流程加了短路兜底。

### 3.2 不同版本之间（这次新增的闸门）

单实例锁有个前提容易被忽略：**它只在「双方都调用它」时才互斥**。
而**自动更新用的那个锁是 v1.2.1 才引入的，v1.2.0 及更早的版本里根本没有这段代码**，
历史安装包也改不了。它们和新版**共用同一个数据目录、同一个数据库文件**
（`%APPDATA%\ai-network-lab\db\custom.db`）—— 只要新旧两个版本同时开着，
就是两个进程并发写同一个库。这就是「用多版本开并发」的口子。

这次从三层把它关掉：

**① 启动时主动查一遍系统（多层闸门）**
新版启动时会枚举同名进程。只要发现有**另一个安装路径**的同名客户端在跑，
立刻弹框告诉你「哪个 PID、在哪条路径」，并且**直接退出**，连解压和建库都不做。

- 判定只看「exe 绝对路径不同」：同路径的第二个实例仍交给单实例锁处理。
  这样升级后自动重启时，旧进程还没退干净也不会把自己卡死在启动对话框上
- 进程鉴别靠命令行特征：排除带 `--type=` 的渲染/GPU 子进程，以及跑 `server.js` 的内部服务子进程
  （本应用是用同一个 exe 以 Node 身份跑内部服务），避免把自己刚拉起的子进程误判成「第二个客户端」
- 查不出来就放行（只记日志），不会因为安全软件挡了进程查询就把你锁在自己的应用外面

> 这里踩了一个很隐蔽的坑，值得说明：**WMI 的进程查询会偶发返回空结果**。
> 实测在一台机器上连续采样 90 秒，进程明明一直在跑，却有两次查到「0 个进程」——
> 而「查不到」在这个问题上恰好等价于「没有冲突」，于是守卫**不报错、不弹窗，就是拦不住**，
> 属于最坏的一类失效（静默）。现在的结果会再用 `tasklist`（走另一条内核快照路径、不经过 WMI）
> **交叉确认**，并且**最多重试 3 次**；只有两边都说「没有」才敢相信。

**② 反向场景：新版开着，你又去点了旧版图标**
旧版内部没有守卫代码，拦不住它自己启动 —— 能做的是**尽快让你知道**。
窗口重新获得焦点时会做一次复检（60 秒节流），发现后弹系统提醒 + 界面横幅，让你关掉它。

**③ 从安装器层面保证磁盘上只有一份**
安装选项 `allowToChangeInstallationDirectory` 已改为 `false`（原先可改）。原因很直接：
允许改目录，你就能把新版装到另一个目录，磁盘上于是同时存在两份不同版本 —— 这正是上面那个口子的入口。
固定后升级会直接覆盖旧版本（NSIS 检出已安装版本时会沿用注册表里记录的目录），**磁盘上永远只有一份**。

> 顺带说明「同一路径但装的是旧版本」这一种：它不归启动守卫管（守卫只看路径是否不同），
> 而是**安装器在覆盖文件之前就解决了**。electron-builder 的 NSIS 会按
> `$_.Path.StartsWith('$INSTDIR')` 找出正在运行的同目录进程，弹「应用正在运行」并结束它 ——
> 否则运行中的 exe 根本覆盖不了。所以「同路径不同代码」也不可能并存。

**④ 数据层面的兜底：不允许旧程序打开新数据**
本机会把程序版本写进 SQLite 文件头自带的 `PRAGMA user_version`（不建表，Prisma 也看不见它）。
启动时如果发现「**库里记录的版本比当前程序新**」，说明这份数据已经被更新的版本写过，
旧代码不认识新增的列和表，继续启动就可能把新数据悄悄写坏 —— 所以直接拒绝启动，并保留全部数据。
反方向（库比程序旧）永远放行，正常升级不受影响。

> **一件需要你做的事**：本次没有在本机找到任何 `1.2.0` 的安装包文件（全盘搜过，只有 git 标签、
> 桌面快捷方式，以及 `%LOCALAPPDATA%\Programs\AI Network Lab` 里已安装的那一份 1.2.0）。
> 请**安装这一版 1.2.4 把它原地覆盖掉** —— 它是唯一一个没有单实例锁的版本，
> 覆盖之后就再没有「新旧并存」的可能了。历史 Release（v1.2.0 / v1.2.1 / v1.2.2）目前都是 GitHub 草稿，
> 建议在 GitHub 上一并删除草稿，避免以后被误下载。

> **验证到什么程度（如实说明）**：单测 152/152、类型与 lint 全绿、安装包内的壳层脚本与仓库源码
> **逐字节一致**（sha256）、前端产物也确认带上了本次全部改动。但**「两个版本同时运行会互相拒绝」
> 这条实机演出没有跑成** —— 开发沙箱不允许启动被测程序、也不允许它 spawn PowerShell/taskkill，
> 守卫在那种环境下只能走「查不到就放行」的分支。该场景的判定逻辑由 19 条单测覆盖，
> 你可以在自己的桌面会话里跑 `node .recon/guard-live-test2.mjs` 复核。

## 四、应用图标

打包日志里那句 `default Electron icon is used` 没了。图标由 `node scripts/make-icon.mjs` 生成：

- `build/icon.ico` 内含 **16/24/32/48/64/128/256 七个尺寸**，Windows 按 DPI 和各显示位置挑最近的那张
- 图案是「中心枢纽 + 四个卫星节点」，深蓝到青的渐变

这里有个实测出来的细节：**同一个图案在小尺寸下会失效**。带连线的完整版缩到 16px 时，
四条连线会和节点糊成一个「十字」，看着像医疗标志而不是网络拓扑。
所以 ≤32px 单独用了一套**去掉连线、放大节点**的简化几何 —— 16px 下能清楚认出是「一个枢纽 + 四个节点」。

---

# 上一版发行说明 v1.2.3

> AI Network Lab —— 智能网络科研工作台
> 当时包版本：v1.2.3（对应 `package.json`）

**本次三件大事：删掉一个实测不可用的功能，把笔记导出真正接进 Obsidian，再把论文写作串成闭环。**

---

## 一、移除「智能组网实验室」（INET）

实测结论：这个功能**跑不起来**，而且有两道互相独立的门槛。

**门槛 A —— 前端从来没有 manifest 配置入口。**
点击「运行场景」时只发送 `scenarioType` 与 `parameters`，没有 manifest；全前端搜索 `manifest` 在组件层**零命中**。
服务端拿到空的 `projectRoot`，`validateManifest` 必然返回诊断 → 任务在到达运行器之前就被判为 failed。
也就是说：**即使你自己把 OMNeT++/INET 装好，也无法从界面跑起来**。

**门槛 B —— 依赖外部工具链。**
它需要 OMNeT++ + INET，这是要自行编译、数 GB 量级的 C++ 工具链。

让使用者为此自建一套重型环境，成本远大于收益，因此**整体移除**：相关 UI、API、库、测试、文档、Prisma 模型全部删除。
老版本升级时，`migrate-database.js` 会把这些功能遗留的空表清理掉（先子表后父表，避免外键阻挡），**你的文献与笔记数据不受影响**。

**组网仿真能力并没有丢**：内置引擎的「组网仿真实验」不需要任何外部依赖，同参数同种子结果完全可复现，是真正可用的那一个。

## 二、导出 Markdown 直接写入 Obsidian vault

以前只能「另存为」得到一份 Obsidian 兼容的 md，你还得手动搬进 vault —— Obsidian 无从索引、无从双链。
现在：

- 设置页新增「**Obsidian 笔记库**」：vault 目录（系统原生目录选择器）+ vault 内子目录 + 开关
- 导出菜单新增「**同步这一篇到 vault**」「**全部文献笔记同步到 vault**」
- 「导出 Markdown」在 vault 可用时**顺便写入**（vault 写入失败不影响下载）
- 导出的 md 带 YAML frontmatter 与 `#标签`，Obsidian 打开即纳入索引

写入路径做了围栏校验：绝对路径 / `..` / 盘符 / UNC 一律拒绝或净化；写入前用 `path.resolve` 二次确认仍在 vault 内。
端到端实测确认：**vault 之外的目录不会被写入**。同名笔记自动追加短 id 后缀，批量同步不会互相覆盖。

## 三、论文写作终于「闭环」了

以前「论文写作」有结构检查、句式、审稿回复、投稿清单，但**没有地方真正写正文**——你在别处写，引用编号、参考文献、格式全靠手工对齐。
现在「论文写作」的第一个标签页是「**写作工作台**」，把链路一次接通：

**文献库 → 插入引用 → 写章节 → 引用自动编号 → 生成参考文献 → 导出**

- **新建即开工**：自动带上 Abstract / Introduction / Related Work / System Model / Method / Experiments / Conclusion 骨架，不用对着空白页发呆
- **写的时候不用管编号**：在右侧搜文献库，点一下就把 `[@paperId]` 插到**光标处**（光标位置会被还原，不用再找刚才写到哪）
- **编号全自动**：按正文**首次出现顺序**编号，删掉一段或调整章节顺序后会重排
- **预览 = 导出**：编号只有一个来源（服务端），所以你右边看到的参考文献，和导出文件里的**是同一份**，不会出现「预览 [3] 导出变 [5]」这种对不上的情况
- **引用错了会告诉你**：`[@id]` 指向的文献在库里不存在时，它**仍然占号**并在面板和导出里标红，而不是悄悄消失
- **不怕丢稿**：停笔 800 ms 自动保存，`Ctrl/Cmd+S` 立即保存；保存失败会保留你刚写的内容并可点重试；切走页面前会把没落盘的改动补写
- **导出 md / txt**：md 带标题层级与规范化的 IEEE 风格参考文献；中文文件名不乱码

字数统计按中英混排分别计数（中文按字、英文按词），并自动忽略 `[@..]` 标记、代码块和链接。

## 四、顺带修掉的两个隐患

1. **INET 运行器一旦启动失败，会把整个后端打崩。** 子进程的 `'error'` 事件没人监听，而 Node 在「可执行文件不存在」时只发 `'error'`、不再发 `'close'`——EventEmitter 无监听者就会抛未捕获异常，进程直接退出。这条调用又在 fire-and-forget 的异步函数里，等于**一次失败的实验让整个内部服务消失**，你看到的是界面突然全废。
2. **测试跑完不退出，会让发布流水线永远卡住。** `vitest run` 在本项目里用例全绿后进程不退出（既有问题，改动前的基线同样复现）。这会让发布的 `Unit tests` 步骤一直挂到 GitHub 默认的 **6 小时**上限，安装包永远发不出来。已改用 `pool: 'threads'` 并给发布任务加超时兜底。

## 五、打包配置里一个会让发版彻底失败的错键（本次才发现）

`electron-builder.yml` 里限制 Chromium 语言包的键写成了 `win.locales`。这个键**不存在**——electron-builder 26 里它叫 `electronLanguages`。

后果比"少了一行配置"严重得多：整段 `win` 配置会被 JSON schema 校验拒绝，打包**当场终止**，而报错只有一句

```
configuration.win should be one of these: null
```

**它不告诉你是哪个字段错了。** 于是 CI 的发布任务会以同样原因失败——**也就是说 v1.2.2 的 Release 从来没有产出过可用安装包**，而失败原因藏在这么一句看不出所以然的话里。

本次已改为 `win.electronLanguages`，并**在本机完整跑通了 `desktop:build` → `electron-builder --win` 全流程**，确认能产出安装包。

---

# 上一版发行说明 v1.2.1

> AI Network Lab —— 智能网络科研工作台
> 当时包版本：v1.2.1（对应 `package.json`）
> 累积自上一个可用版 v1.1.0（桌面安装包 v1.1.0）

本版是一次**累积功能 + 打包可靠性修复**的发布。相对 v1.1.0，新增了「Zotero → 本软件 → Obsidian」科研工作流的补齐，并集中修复了桌面端解压/升级链路的隐患，同时把安装包与运行时体积砍掉约七成。

**强烈建议升级，尤其是桌面版用户。**

---

## ⚠️ 本次最需要关注的修复

### 1. 解压逻辑由"先删后解压"改为 fail-safe 原子替换

**风险（旧实现）**：`ensureAppExtracted()` 先删除旧的 `resources/app` 目录，再解压新 `app.zip`。一旦解压中途失败（磁盘满、被杀进程、压缩包损坏），旧目录已删、新目录不全——**应用直接打不开**，且无法自愈。

**修复**：解压到临时目录 `app.new` → 校验通过 → 原子改名替换，并写入 `.extract-ok` 完整性标记。任一步失败都保留原目录，安装始终处于可用状态。

### 2. 安装包与运行时体积大幅下降

| 项目 | 修复前 | 修复后 |
| --- | --- | --- |
| Next standalone 产物 | 607 MB | **92.9 MB** |
| `resources/app.zip` | 87 MB | **31.2 MB** |
| Chromium 语言包 | 55 个（约 48 MB） | 2 个（zh-CN / en-US） |
| 构建期误拷 `test-results` | 366 MB | 已挡在 tracing 之外 |

- `prepare-standalone.js` 由黑名单改为**白名单裁剪**：顶层只保留运行必需项，递归清除 `*.tmp*`、非 sqlite 的 `wasm-base64`、`*.map`、`*.d.ts`。
- `next.config.ts` 的 `outputFileTracingExcludes` 补挡 `test-results` / `playwright-report` / `e2e` / `tests` / `docs` / `.github` / `db` / `*.db`。

---

## v1.2.0 新增功能（本版一并包含）

### 补齐「Zotero → 本软件 → Obsidian」科研工作流
导入/同步**不会**自动调用 LLM。

- **论文阅读笔记导出 Markdown / PDF / TXT**：论文详情页「导出笔记」走 `/api/papers/:id/notes`
- **科研笔记服务端导出 MD/PDF**：`/api/notes/export/:id`，桌面端不再依赖前端 Blob 拼文件
- **论文库 CSV 是论文列表**：UTF-8 BOM + 题名/作者/期刊/年份/DOI/状态/标签
- **导入 RIS / BibTeX**：按 DOI / Zotero key / 标题去重合并
- **导入 PDF 入库**：PDF 落到本机 `library/pdfs/`，可挂到已有论文或新建条目；不解析、不送 LLM
- **Zotero Web API 同步**：设置页填写 User ID + API Key，一键拉条目元数据

### 其他
- 桌面端升级时对 `Paper.doi` / `zoteroKey` / `pdfPath` 做无损 `ALTER TABLE`

---

## 工程卫生

- 移除已完全合并的 `feature/inet-lab` 工作树与分支
- 归档并移除两个早期废弃仓库（初版四 demo 之一的 `AI-net`、更早的 `智谱AI` 分支）

---

## 安装

### 桌面版（推荐）
```bash
npm run desktop:dist        # 重打安装包 → release/AI Network Lab-Setup-1.2.1.exe
```
安装后直接使用；升级时研究数据（`%APPDATA%\ai-network-lab\db\custom.db`）自动保留，旧代码会在启动时按 `BUILD_ID` 对比被安全替换。

### Web 版
```bash
npm install
npm run db:push             # 初始化数据库（含新字段）
npm run dev                 # http://localhost:3000
```

## 使用说明
应用内「使用说明」功能区覆盖完整上手指引（快速开始 / 接入 AI / API 速查 / 数据备份 / FAQ），也可从新手引导直达。
