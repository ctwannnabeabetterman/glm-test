# 发行说明 v1.3.10

> AI Network Lab —— 智能网络科研工作台
> 当前包版本：v1.3.10（对应 `package.json`）

**补上 1.3.9 缺的那一半：AI 能改分数，你也得能改回来。**

上一版刚上「AI 重评阅读优先级」，承诺了「可人工覆盖」；但自查时发现论文详情里**相关度 / 新颖度是只读的**
（那里的编辑态只服务于笔记）—— 也就是说 AI 把分数写回之后，你在界面上**改不回来**，承诺的一半是空的。

这一版补上入口：

- 论文详情里新增「**改分数与优先级**」：相关度、新颖度（1-10）与优先级（高/中/低）就地可改；
- 保存用的是同一条接口，并**顺带摘掉该篇的「AI 评」标记**（提示会明确告诉你这件事已完成）；
- 详情页会写清**这个分数是谁给的**：「这项评分由 AI 按题录与摘要给出（时间）」，或者「当前是你自己填的分数」；
- 改完徽标**当场消失**，不用重进页面。

验证：单测 **40 个文件 / 738 条用例**全过，类型检查与 lint 零输出。

---

# 上一版发行说明 v1.3.9

> AI Network Lab —— 智能网络科研工作台
> 当前包版本：v1.3.9（对应 `package.json`）

**这一版把论文库里的「判断」接到了 AI 与写作上：阅读优先级可以交给 AI 按论文信息重评，引用追踪里的关系可以直接变成稿子里的引用。**

---

## 一、AI 重评阅读优先级

你说「论文库的优先级排序……也是 AI 基于论文信息整合的」。查下来实际情况是：排序用一套**固定公式**
（相关度 × 0.4 + 新颖度 × 0.3 + 开源代码 × 15 + 年份项 × 0.5 + 优先级 × 2），而这四个输入值**靠建库时手工填** ——
填过一遍之后基本没人回头改，于是那张决定「今天读哪篇」的榜会很快不再反映真实优先级；AI 只在「相关论文」面板里
给过高/中/低建议，但从来不写回库。

现在「优先级排序」页顶部多了一个 **AI 重评** 面板：

- 选 **课题域**（相关度是相对课题才有意义的量）+ **重评范围**（默认「仅未读」）+ 最多评几篇 → 点「让 AI 打分」；
- AI 按**题录 + 摘要**逐篇给出新的相关度 / 新颖度 / 优先级与一句理由，界面把「当前 → 建议」和理由都摆出来；
- 你**勾选后再点应用**才写回 —— 刻意不做「一键全改」：模型的分数可能整体偏高，静默覆盖你自己填过的判断比不做更糟；
- 写回后的论文在列表里带 **AI 评** 徽标（悬停看打分时间）；**你手工改过某篇的分数，徽标会自动摘掉**，
  标记不会在你改完之后继续说谎；
- 分数徽标悬停能看到**构成拆解**（相关度 5×0.4=2.0 …）—— 公式只维护一份，以后不会再出现「显示的分数与排序依据不一致」。

顺带说一个刻意的取舍：「这条分数是 AI 给的还是你填的」这个来源标记放在一条设置项里，**没有改数据库结构** ——
所以从旧版本升上来不会动你的数据。

## 二、引用追踪 → 一键插入稿件

以前看得到「谁引了谁」，却没法把这份信息用进正在写的论文里。现在三处都能一键插入：

- **引用追踪**：引用排行可以**一次把前 5 篇合成一处引用**送进稿子；每条关系都能一键引用「被引方」；
- **关系网络**：一条关系＝两篇一起引（共享标签 / 同作者往往正是你一起引它的理由）；
- **论文详情**：BibTeX 卡片旁直接「插入稿件」。

点一下就把 `[@论文id]` 送进写作工作台，落到 **Related Work 章节末尾**，并自动切到写作页让你**当场看到落点**
（toast 会明说落在哪一章）。编号与参考文献表照旧由写作工作台统一生成，IEEE / GB-T 7714 任选。
再点一次是**追加**，不会覆盖已经写好的内容。

## 三、顺手修掉一个「数字与列表自相矛盾」

「引用追踪」页曾经会显示「引用关系 3」而列表区写着「暂无引用关系」—— 因为接口会过滤掉**指向已删除论文**的关系，
但统计用的是过滤前的条数。删论文并不会连带删除引用关系，所以这种孤儿关系很容易积累，而用户完全看不出原因。
现在统计口径与列表一致，并如实提示「另有 N 条关系的论文已不在库中」；引用排行也只按确实可见的关系计算。

## 四、验证

- 单测 **40 个文件 / 734 条用例**全过（上一版 36/654）；类型检查与 lint 零输出。
- 真机验证（无头 Edge + 真实库数据）**21 项全过**：AI 面板的请求确实发出，且错误来自 LLM 层（本机未配 API Key）
  而不是参数问题 —— 这反过来证明取料路线在真实库上跑通了；点「插入引用」后确实切页、写入 `[@id1, id2]`、
  **第二次是追加**（稿件 55 → 85 字，旧内容仍在）；孤儿关系的提示在真机上可见。
- 验证用的临时稿件与临时引用关系已删除，并以「接口 + 直查数据库」双重复核，库回到原始状态。

---

# 上一版发行说明 v1.3.8

> AI Network Lab —— 智能网络科研工作台
> 当前包版本：v1.3.8（对应 `package.json`）

**这一版把「结果长什么样」和「引用怎么落地」交给你：AI 输出能按阅读场景换密度，参考文献能按 IEEE 或 GB/T 7714 著录，综述不再是只能看的文本、而是一键进写作台的草稿。**

---

## 一、结果呈现密度：在设置里选（你提的）

你说「可以设计结果界面在设置供用户选择」。我选了复用度最高、风险最低的那一层：**呈现密度与排版** —— 只改 AI 输出的字号与行高，不动布局、不动数据。

设置页新增「界面与阅读」一栏，三档：

| 档位 | 效果 | 适合 |
|---|---|---|
| 紧凑 | 字号 ×0.92、行高 1.5 | 一屏看更多结论 |
| 标准 | 与界面其余部分一致（默认） | 日常使用 |
| 论文式 | 字号 ×1.08、行高 1.9、正文换宋体 | 长段阅读、直接抄进稿子 |

- 选择**立即生效**，而且**已经生成的结果直接跟着变**，不需要重跑 AI。
- 三档都带一张真实的 AI 输出样张做预览 —— 用渲染器本身画的，不是另做的示意图。

## 二、写作即引用：IEEE 与 GB/T 7714 两套著录格式

写作工作台早就能把正文里的 `[@论文]` 自动编号、生成参考文献表，但**格式写死成 IEEE 一种** —— 投国内期刊要 GB/T 7714，就得手改整张文献表。

- 右栏新增**著录格式**下拉：**IEEE** 与 **GB/T 7714** 任选，预览立刻变，导出（md / txt）用的是同一格式。
- 两种格式对姓名的要求正好相反（IEEE 缩写在前 `J. Smith`，GB/T 姓在前 `SMITH J`），而你库里存的作者串是「姓, 名」——所以这一版真做了姓名解析与重排。**拿不准的一律原样输出，不猜**：格式不标准还能改，姓和名颠倒了是要出勘误的。
- GB/T 这边补齐了规范要求的几项：文献类型标识 `[J]/[C]/[M]/[D]`、超过 3 位作者只列前 3 位再加「等 / et al.」、缩写点省略（`SMITH J`）。
  **库里没有的字段（卷、期、页码）不会编造**，界面上直接说明这个缺口，投稿前你自己补齐。
- 顺手修掉一个既有小毛病：库里有些条目把年份写进了期刊名（`IEEE TCOM, 2019`），以前导出会变成「IEEE TCOM, 2019, 2019」。
- 格式是**全局偏好**，不改数据库结构 —— **升级不会动你的数据**。

## 三、阅读 → 综述草稿（新的独立标签）

原来写作页里的「AI 文献综述」有个根本问题：它取的是**全库最新 15 篇**，跟你读过什么、属于哪个课题都无关；引用写成 `[作者, 年份]` 这种自由文本，与文献库没有任何关联，既不能自动编号也不能生成参考文献表。

现在它是「阅读 → 综述草稿」：

- **取料看两件事**：**课题域**（只用作挂到该课题的文献）× **阅读范围**（默认**仅已读**）—— 综述的依据应该是你确实读过的那些。
  面板会告诉你「还有 N 篇因不在范围内被排除」，想纳入就把它们标记为已读。
- **产出带真引用**：正文里是 `[@论文]` 标记，与写作工作台同一套语法，可被自动编号。
- **一键「发到写作工作台」**：直接插成新章节，引用自动编号、参考文献表自动生成 —— 不用再复制 → 切页 → 找章节 → 粘到光标处。
- **会告诉你模型有没有编引用**：生成后逐条比对文献库，查出清单外的引用就当场点出来。这种错在导出前几乎不可能被发现。
- 范围内没有文献时**直接拒绝并告诉你下一步怎么做**，而不是硬编一段综述出来。

## 四、验证

- 单测 **36 个文件 / 654 条用例**全过（新增 3 个文件、66 条）；类型检查与 lint 零输出。
- 真机验证（无头 Edge + 真实库数据）跑了 **33 项**：在真实题录上切换格式，IEEE（`Jason Uher, …`）与 GB/T（`UHER J, WYSOCKI T A, … Review of distributed beamforming[J]. …`）**互斥生效**、导出徽标与持久化同步；
  综述面板的课题/范围选择器与「生成」按钮的禁用→可用状态机逐项验过，点击后请求确实发出，且返回的错误来自 LLM 层（本机未配 API Key）而**不是**参数错误 —— 反过来证明课题域 + 仅已读的取料链路在真实库上完整跑通了。
- 验证用的临时稿件已删除并双重复核，库回到「0 稿件」的原始状态。

---

# 上一版发行说明 v1.3.7

> AI Network Lab —— 智能网络科研工作台
> 当前包版本：v1.3.7（对应 `package.json`）

**这一版做三件事：界面重做（科研风）、跑得更快更小、防止被逆向破解。**

---

## 一、界面重做：从「像 demo」到「能长时间读」

你说「主要功能具备了，但是还是像 demo」，并要求「科研风的页面排版」。查下来的结论是：
**这不是配色问题，是排版语法的问题** —— 具体是四条：

| 问题 | 实测数据 | 为什么显得像模板 |
|---|---|---|
| 字号全线偏小 | 9px 的 16 处、10px 的 126 处、12px 的 263 处 | 正文普遍 10–12px，那是「控件说明面板」的密度，不是阅读密度 |
| 卡片套卡片 | 每个页面都是「卡片 > 卡片头 > 卡片内容」 | 屏幕被切成等大方块，视觉上没有主次 |
| 装饰过载 | 7 处渐变、7 处 hover 上浮阴影、脉冲绿点、渐变标题 | 那是营销落地页的语言，不是工具 |
| 首屏被零值占据 | 4 个统计卡显示「0 已读 / 0 评审」 | 还没数据时，这些卡是纯噪音 |

**改法**：

- **正文换成宋体（Noto Serif SC），界面元素用黑体**，数字用等宽。正文 14px、长文 15px/行高 1.75、页标题 24px。
  顺带修掉一个真 bug：之前字体是从 Google Fonts 在线拉的 —— 这是个**离线桌面应用**，断网时字体会拉不到。
- **配色收敛**为纸感暖白 + 墨色文字 + 单一墨蓝强调色；深色主题改成阅读器夜间模式。彩色渐变全清。
- **12 个页面一次改完**：**142 处**过小字号提级，清掉全部装饰性渐变与上浮阴影，交互反馈改用边框高亮表达。
- **首屏（总览）改成论文式题头 + 摘要段落**；统计条改成细分割线网格，并且**全为 0 时整块收起**；
  「三层架构」从彩色渐变卡改成学术表格形式的线性表达。
- **侧边栏**：导航从「中文 + 英文双行」压成单行中文（英文降级为悬停提示），顶栏从 14 行压到 12 行，
  新增面包屑，版心统一 1180px。

## 二、跑得更快、装得更小

**首屏加载**：这是个单页工作台，以前 12 个页面是「全部一次性打包」的 —— 你打开应用哪怕只想看首页，
也得先下载并解析全部 12 个页面的代码。实测首屏前端 JS 有 **1.8 MB**，其中图表库（recharts）一块就占 **1.1 MB**。
现在每个页面是独立小块，**用到才加载**；图表更是「统计区没数据时根本不去请求」。

**安装包瘦身 20%**：查出一处很隐蔽的体积泄漏 —— 打包清理逻辑跑在「符号链接解引用」**之前**，
而符号链接目录在遍历时看起来是空的，于是「清理过了」的假象下，解引用之后 **73.4 MB 的 Prisma 运行时**
又整个回来了。实际需要的只是其中 **1 个文件（197 KB）**，其余是 5 种数据库 × 2 种模块格式的 WASM 与四套用不到的平台运行时。
另有一个 11 MB 的 pdfkit 包，里面混着 Yarn 的构建工具链（光 yarn 本体就 2.9 MB）。
解压后的产物从 **120.9 MB 降到 25.3 MB**，**安装包从 144.2 MB 降到 114.8 MB（小 29.4 MB，约 20%）**，
首次启动解压也更快。

**顺手把后端源码保护起来**：Next 默认会给每个接口生成 `.js.map`（那种文件里内嵌**完整的原始 TypeScript 源码与注释**），
实测有 191 个。已关闭 —— 逆向的人只能读到压缩后的产物。

## 三、防逆向破解：堵掉几条「顺手就能用」的路

先说清楚边界：**这不是「无法破解」**。Electron 应用的代码最终必然在你机器上，做不到「绝对防破解」。
能做的只有把最容易被顺手利用的几条路一条条堵上。这版堵了：

- **DevTools 关死。** 以前按 F12 / Ctrl+Shift+I 就能开出开发者工具 —— 那意味着**能直接读内存里的 API Key、
  能改前端逻辑绕过校验**。现在双层拦截：已开出来的强制关闭 + 快捷键本身拦掉。开发构建不受影响。
- **窗口导航白名单。** 当前窗口只允许停留在本地服务上，跳转到外部地址会被拦下并交给你系统里的浏览器打开。
  （页面里的论文链接、检索链接不受影响，它们本来就走系统浏览器。）
- **权限全拒。** 摄像头、麦克风、定位、通知、剪贴板读取、USB/串口访问 —— 一律拒绝。科研工作台不需要这些。
- **内容安全策略（CSP）。** 限制页面只能加载同源资源、只能连接本地服务，禁用 `eval` 一类动态求值。
  （唯一的例外是内联脚本必须放行，这是 Next 框架的引导脚本所必需；解释写在源码注释里。）
- **加固代码集中在 `desktop/hardening.js` 一处**，便于审阅；另加 26 条单测锁住行为，
  例如「开发态不允许关 DevTools」（否则你自己没法调试）。

## 本版验证

- `tsc --noEmit` 0 错误 · `eslint` 0 问题 · `vitest` **32 文件 / 573 用例全过**
- CSP 不是「写了就算」：拉取 9 个前端代码块逐一扫描，确认没有 `eval` / `new Function`、没有外部样式引用、
  唯一一个指向 CDN 的图标已改成本地文件 —— 这三条是敢把策略收这么紧的依据
- 体积裁剪在真实产物副本上干跑并逐项断言（必需文件都在、废料都清），并做了**功能验证**：
  裁剪前后各真生成一次 PDF 都成功（文件头 `%PDF-1.3`）
- 发布后核对：安装包 / blockmap / latest.yml 三件齐全；latest.yml 声明的 `size` 与资产
  实际字节数一致（120,410,768）；`/releases/latest` 已指向 v1.3.7；无孪生 Release

---

# 上一版发行说明 v1.3.6

> AI Network Lab —— 智能网络科研工作台
> 当前包版本：v1.3.6（对应 `package.json`）

**这一版做两件事：把「自动更新」的过程讲清楚，以及让 AI 功能拿到它该拿的原料。**

你连续提了三条反馈，本版逐条回应：

1. **「不知道怎么下的，需要重启安装，然后安装没有进度条之类的」** → 见第一～四节
2. **「打分应该是 AI 打的吧，自己打分有点难」** → 见第五节
3. **「AI 研究分析应该要加一个课题选择，多个同时分析内容少不说还容易在课题多了以后互相干扰」** → 见第六节

---

## 一、你反馈的更新三件事，逐条说清

这三件事查下来，只有一件能在架构上修，另外两件只能把话说清楚 —— 下面如实说明。

| 你说的 | 真相 | 这版怎么做 |
|---|---|---|
| 不知道怎么下的 | 下载进度**一直有数据**，但只显示成一个数字，还藏在设置页和窗口标题里 | 加真正的**进度条 + 传输量 + 速度 + 约剩多久**；下完直接告诉你**安装包存在哪** |
| 需要重启安装 | 这是更新机制的固有行为：安装要替换正在运行的程序文件，文件被占用就写不进去 | 改不掉，但会把「为什么要重启」「要等多久」提前讲清楚 |
| 安装没有进度条 | **架构上给不了**（见下节），不是坏了 | 新增**安装前确认框**，在窗口消失之前把会发生什么说明白 |

## 二、为什么「安装时带进度条」做不到

这不是偷懒，是 electron-builder 的安装器模板定死的：

- 应用内点更新后，程序以 **`/S`（静默）** 方式调起安装包。**静默安装本身不绘制任何界面**，
  所以系统里根本不存在「安装进度条」这个东西给我们读。
- 更关键的是：**点下按钮后应用立刻就退出了**。等安装程序开始干活时，我们的进程已经没了 ——
  界面连一次刷新机会都没有。所以任何「安装中 37%」的显示都会是假的。
- 那为什么不干脆换成**非静默**安装（这样就有向导和进度条了）？因为模板里
  「装完自动把应用拉回来」的条件是 **`${isForceRun} AND ${Silent}`** ——
  **只有静默安装才会自动重开应用**。换成非静默，代价是「装完应用不回来，还得自己去找图标」，
  比现在更糟。

**所以这版选了保守方案：不动安装行为，把预期讲在前面。**

## 三、安装前的确认框（新增）

以前点「立即重启并安装」，窗口直接消失，中间没有任何解释 —— 只能靠猜。
现在退出之前会先弹一个框，写清楚：

```
接下来会发生的事：
1. 应用窗口会立即关闭；
2. 安装程序在后台自动完成（静默安装，不会显示进度条，这是正常的）；
3. 大约 10–60 秒后应用会自己重新打开，版本即为新版。

安装期间请不要手动结束安装程序。如果一分钟后应用没有回来，
可以手动打开「AI Network Lab」图标；版本还是旧的就去发布页手动下载。

安装包位置：<你机器上的完整路径>
```

选「稍后」就完全不安装，应用继续用，不会把你晾在半路。

## 四、下载过程现在看得见了

下载中（右下角常驻卡片和「设置 → 软件更新」两处一致显示）：

```
正在下载更新 42%
60.0 / 140.0 MB · 2.0 MB/s · 约剩 40 秒
[████████░░░░░░░░░░░░]
下载期间可以继续使用，不影响写作。
```

以前只有一个「42%」在跳，你没法判断是在慢慢下还是已经卡死了 —— 这正是「不知道怎么下的」的来源。

下载完成后会显示：

```
新版本 1.3.6 已就绪 · 点「立即重启并安装」生效（应用会自动重开）
安装时会静默进行、不显示进度条：窗口先关闭，约 10–60 秒后自己回来，属正常现象。
安装包已存于：C:\Users\...\ai-network-lab-updater\pending\AI-Network-Lab-Setup-1.3.6.exe
```

## 五、顺带修掉的一个排查盲区

以前**下载进度在日志里一行都不留** —— `update.log` 里只有「开始下载」和「下载完成」两条。
实测首次下载花了 **3 分 20 秒**，事后翻日志完全看不出这三分半是一直在慢慢下、还是中途卡了很久。
现在按 10% 一档落盘，下次真出问题能对着时间轴看。

## 五、AI 帮你打分：选题评估不再让你从一片 5 开始（新增）

你的原话：**「打分应该是 AI 打的吧，自己打分有点难」**。

选题评估有 4 个维度、13 个细项，以前全靠你逐项拖滑杆，新建课题还一律预设 5 分 ——
点开「打分」看到一片 5，既不知道填得对不对，也懒得逐项调。

现在点一下 **「AI 打分」**，AI 会把整张表铺好，你只改不认同的那几项。

### 但有一件事它故意不做

**它不是「AI 替你决定这个课题好不好」。** 三个刻意的设计：

1. **只算分、不落库。** 拿到 AI 的分数后，仍然走你手动保存的那条路径写进数据库 ——
   你改过哪几项、什么时候改的，全都留在同一条数据路径上。
2. **区分「AI 判得了的」和「只有你知道的」。** 下面这 4 项是**私有事实**，
   AI 无从知晓，只能给个「同类学生的常见情况」：
   - 数据/仿真平台可得性
   - 计算资源需求
   - 个人能力匹配度
   - 是否与毕业论文方向一致

   它们在界面上带一个**「确认」徽章**，提示词里也明确要求 AI 在理由里注明「这是估计」。
   不这么做的话，它可能用很确定的语气说「你匹配度 8 分」，而你看了会以为自己确实匹配。
3. **打分依据只限本课题。** 不拿全库数据给一个课题打分 —— 那会把别的方向的趋势算进来。

### 它会告诉你分是怎么来的

AI 打分结果上方有一块说明：

```
AI 打分依据
本次依据 12 篇论文、5 条笔记（另有 7 篇论文未归到本课题，未参与打分）。
已填 13/13 项
带「确认」标记的 4 项 AI 无从知晓（实验室条件、你的时间与方向），
它是按同类情况估的，请按实际改写。
```

如果一个课题还没挂任何论文和笔记，这里会直接说「本次依据 0 篇论文、0 条笔记」——
**让你知道 AI 是凭课题名硬猜的**，而不是以为它「读了资料」才给的分。

## 六、AI 研究分析现在按课题来（新增）

你的原话：**「AI 研究分析应该要加一个课题选择，多个同时分析内容少不说还容易在课题多了以后互相干扰」**。

这条说到点子上了。旧实现其实有**三个叠加的问题**：

1. 请求体里**根本没有课题这个概念** —— 所有分析都是对全库做的；
2. 把**全部课题**一起塞进上下文，方向一多，模型的注意力被摊薄；
3. 最要命的：取料用的是「**全库最新的 20 篇论文**」——
   既与课题无关，又会把**你这个课题相关但年份较老的文献直接挤掉**。

### 现在怎么改的

- **顶部多了一个课题选择器**，默认选中第一个课题；也有「全库综合分析」选项保留旧能力
- **给论文和笔记加了「所属课题」字段**，在论文详情页 / 笔记页直接勾选（可多选，勾了即存）
- 分析**只基于本课题的料**，且**按相关度取而不是按年份取** —— 年份不再是「谁进得来」的决定因素
- 结果头部如实标注 `课题名 · N 篇 / M 条`，你能立刻判断这次结论有多少料撑着
- **换课题后旧结果不会串台**：结果按课题分别缓存，切回去还在，不会把 A 课题的结论显示成 B 课题的

### 一个重要的诚实：没料就不分析

如果一个课题下面既没有挂论文、也没有笔记，**它不会硬编**，而是明确拒绝：

> 课题「XXX」下还没有关联的论文或笔记。请先在文献库给论文挂上这个课题
> （或在课题描述里补充关键词），再回来分析。

以前无论有没有料都会调模型，模型只能凭记忆编 research gap ——
**这正是「AI 说了一堆但都是空话」的来源**。没有原料就不该假装有结论。

### 附：课题卡片现在显示「有多少料」

课题列表里每个课题会显示 `12 篇 · 5 笔记` 徽章；如果一个课题是 `0 篇 · 0 笔记`，
徽章会变成橙色警示 —— 免得你点到 AI 分析才发现这个课题根本没料。

> **怎么让课题「有料」？** 两条路都行，**任一命中即算**：
> ① 在论文/笔记上手动挂到该课题（最准，推荐）；② 让课题名称/方向/描述里出现能匹配论文
> 标题或标签的关键词（兜底，中英不对应时会漏，所以别只靠它）。

## 七、顺带修掉的两个排查盲区

1. **下载进度在日志里一行都不留** —— `update.log` 里只有「开始下载」和「下载完成」两条。
   实测首次下载花了 **3 分 20 秒**，事后翻日志完全看不出这三分半是一直在慢慢下、
   还是中途卡了很久。现在按 10% 一档落盘，下次真出问题能对着时间轴看。
2. **AI 分数为空时会被静默当成「0 分」** —— 模型把某一项写成空字符串时，
   代码里的 `Number('')` 会得到 `0` 而不是「无效」。而 0 分在评分矩阵里的含义是
   「教科书内容 / 无任何参考实现」，是极强的负面判断，不该由一次空值事故产生。
   现在这种情况判为「该项没拿到」，走 5 分兜底并如实告诉你，而不是悄悄记成 0 分。

---

### 本版验证

- `tsc --noEmit` 0 错误 · `eslint` 0 问题 · `vitest` **31 文件 / 519 用例全过**
- 新增 3 个测试文件：AI 打分容错（`topic-ai`）、课题域筛选（`topic-scope`）、
  接口契约（`topic-routes-contract`，**真实调用 handler 而不是静态扫描**）
- 接口契约里有几条是「守门」断言：无材料时 `chatComplete` **不得被调用**、
  非课题相关的论文**不得进入提示词**、非法 `type` 直接 400

## 上一版发行说明 v1.3.5

> AI Network Lab —— 智能网络科研工作台
> 当前包版本：v1.3.5（对应 `package.json`）

**这一版修「AI 把检索结果整段复制回来」。**

---

## 一、先纠正一个判断：问题不在模型不听话，而在它没有原料

之前遇到 AI 回答又长又像原文搬运时，直觉是「模型太懒 / 提示词没约束住」。
这版排查后发现问题在更前面一步：**论文的摘要在库里是「笔记」**。

`Paper.notes` 这一列原本被两种完全不同的东西共用：

| 谁在用 | 内容 | 语义 |
|---|---|---|
| 你自己 | 阅读时写的心得 | 你的产出 |
| Zotero / RIS / BibTeX 导入 | 论文摘要原文 | 别人的原文 |

两者语义不同（导出、展示、覆盖策略都不一样），混在一列的直接后果是
**下一次同步会把其中一方冲掉**。而 AI 摘要功能去读这一列时，读到的往往不是摘要 ——
于是它只能对着标题和作者「按套路编一段摘要出来」。**看起来像模型偷懒，实际是没料可下锅。**

## 二、把原料补上：`Paper.abstract` 独立成列

摘要与笔记从此各归各位，三处历史混写行为一并修掉：

- **Zotero 导入**：摘要进 `abstract`，不再被写进 `notes`
- **RIS 导入**：`AB`（摘要）与 `N1`（笔记）分开落库
- **BibTeX 导入**：`abstract` 与 `note` 同样分开

合并逻辑遵循「已有内容优先保留」，不会因为重新导入就覆盖你手写的笔记。
新增的 `MergeStats.abstracts` 会在合并结果里如实报告补进了多少条摘要。

## 三、把 AI 接到真实的检索源上

光有摘要还不够 —— 「相关论文」这类问题需要**事实来源**，否则模型只能凭记忆背标题和 DOI，
背错一个字母就变成一条查不到的假文献。

新增 `src/lib/library/retrieval.ts`，真打 **Crossref** 的 `api.crossref.org/works`。
几处刻意的取舍：

- **没有 DOI 的结果直接丢弃。** DOI 是唯一能证明「这篇确实存在」的锚点，
  为了凑数留一条查不到的条目，比少给一篇更糟
- **结果条数夹在 1..20**，不让一次请求把上下文撑爆
- **12 秒超时**，不把界面挂在一次网络请求上
- **任何环节出错都收敛成空数组，绝不抛** —— 检索是「增强」，不该成为新的故障点

`/api/ai-related-papers` 的提示词随之改成硬约束：**只能从这份清单里挑选**，
标题、作者、年份、期刊、DOI **必须逐字照抄**，不许改写、不许补充清单里没有的条目。

响应新增 `source` 字段，如实标注这批结果是来自真实检索（`crossref`）
还是模型的既有知识（`model-knowledge`）。前端据此区别显示：
前者给可点击的 `doi.org` 链接，后者明确提示「请自行核实」。
**有真东西时给真东西，没有时不装。**

## 四、顺手修掉的两个「安静的错」

### 1. 通知接口挂掉时，界面谎报「一切就绪」

`/api/notifications` 返回 500 时，通知铃铛显示的是绿勾 +「暂无通知 · 一切就绪！」
—— 因为拉取失败的 `error` 状态从来没有被读取。

这比单纯的报错更危险：**它把「接口坏了」伪装成「确实没有待办」**，
你会以为自己没有逾期提醒，而不是怀疑程序出了问题。现在改为显式失败态：
琥珀色警示 + 失败原因 + 重试按钮。

### 2. 数据库模板与 schema 漂移会导致启动即报错

`resources/db-template/custom.db` 是按**文件**分发的。它一旦落后于 `prisma/schema.prisma`
（这次就缺了 `Paper.abstract`），Prisma Client 一读就抛
`P2022: column main.Paper.abstract does not exist`，整个通知接口 500。

新增 `.recon/sync-template.mjs` 做逐表核对：把 schema 里每个模型的字段与模板实际列集合比对，
失配时先备份（`VACUUM INTO`）再补列。

## 五、验证

| 项目 | 结果 |
|---|---|
| `tsc --noEmit` | 0 错误 |
| `eslint .` | 0 问题 |
| `vitest` | **27 文件 / 402 用例全过** |
| `verify-planner` 端到端 | **158 通过 / 0 失败** |

### 关于本轮 6 条失败的说明

全量验证时端到端脚本有 6 条失败，表面看是「通知反向联动坏了」。
追下去是**验证脚本自己的盲区**：它复制模板后直接起服务，跳过了桌面端启动时会跑的
`migrateDatabase`；而长期跑 dev 的本机库早就补过列，所以只有这个临时库缺列。

**产品代码没有因此改动**，只是额外补了一条断言：接口整体 500 时不能再安静过去。
如实记在这里，是因为这类失败最容易被误读成产品缺陷。

---

# 上一版发行说明 v1.3.4

> AI Network Lab —— 智能网络科研工作台
> 当前包版本：v1.3.4（对应 `package.json`）

**这一版修「装了 1.3.3 之后应用根本打不开」。**

---

## 一、问题：安装包本身是坏的，对**所有**用户都是

1.3.3 装完之后，双击图标会依次看到两个不同的报错：

```
① AI Network Lab 启动失败
   内置服务解压失败：... tar.exe -xf ...app.zip ...
   .next/node_modules/pdfkit-<hash>: Can't create ...: Invalid argument
   .next/node_modules/@prisma/client-<hash>: Can't create ...: Invalid argument

② AI Network Lab 启动失败
   内部服务启动超时，请稍后重试（或检查内部服务日志）
```

这两条看着无关，其实**同一个根因**。

## 二、根因：打包产物里带着指向「构建机」的符号链接

Next 的构建流程（`outputFileTracing`）会在 `.next/standalone/.next/node_modules/` 下，
为几个被外部化的依赖创建**指向构建机项目根 `node_modules` 的绝对符号链接**。
1.3.3 的包里实测是这两条：

```
.next/node_modules/pdfkit-<hash>          -> //?/D:/a/glm-test/glm-test/node_modules/pdfkit
.next/node_modules/@prisma/client-<hash>  -> //?/D:/a/glm-test/glm-test/node_modules/@prisma/client
```

`D:/a/<org>/<repo>` 是 GitHub Actions 的 Windows 构建机上的固定工作目录 ——
**所以这个缺陷只在 CI 打包时产生，开发机上完全看不出来。**

而打包用的 `bsdtar` 会把符号链接**原样**存进 zip。到了你的机器上，`D:/a/glm-test/...`
当然不存在，解压时建不出这个链接，于是**退化成 0 字节的空文件**。接下来就是多米诺：

```
require('@prisma/client-<hash>') 拿到一个空模块
  → TypeError: r.PrismaClient is not a constructor
  → GET /api/settings/llm 返回裸 500（这条路由没有 try/catch）
  → 壳层探针只认 statusCode < 500，永远等不到成功
  → 90 秒后弹出「内部服务启动超时」，应用退出
```

第 ① 条 `Invalid argument` 就是「建不出这个链接」的报错；1.3.3 里加的退避重试让解压那一步
侥幸过去了，应用随即换第 ② 条症状继续死。

## 三、为什么之前的检查没抓住它

**它能骗过字节级校验。** 把 `app.zip` 解到干净目录再逐文件比 SHA-256，会得到「完全一致」——
因为两边同样是 **0 字节的空文件**，哈希当然一样。同理 `resources\app\` 里的文件也「都在」。

唯一能暴露它的办法是**真的把内部服务跑起来，探一次 `/api/settings/llm` 要求返回 200**。

## 四、修复

- **打包前把符号链接落成真实副本**（`dereferenceSymlinks()`）。断链不能一删了之
  （删掉就变成 `Cannot find module`），改为收集后直接**拒绝出包**。
- **对最终 zip 再加一道硬断言**：产物里符号链接条目必须为 0，否则打包失败。
  宁可这一版发不出去，也不再发一个所有人都装不起来的包。

## 五、如果你已经装了 1.3.3：不用重装也能救回来

1. 关掉 AI Network Lab 的所有进程；
2. 打开 `<安装目录>\resources\app\.next\node_modules\`，把里面那两个 **0 字节的文件**删掉；
3. 从**同一棵目录树**里把 `node_modules\pdfkit` 与 `node_modules\@prisma\client`
   整目录复制进去（后者 41 个文件 / 14.55 MB，**必须包含 `runtime\query_engine-windows.dll.node`**）；
4. 重新打开应用即可。

> 注意别让应用重新解压，否则会被坏 zip 覆盖回 0 字节。

## 六、验证

- 新增 `tests/desktop/prepare-standalone.test.ts`（8 例），含 1.3.3 真实 `tar -tvf` 输出的回归指纹
- 全量单测 22 个文件全通过，`tsc --noEmit` 零错误
- 本机实起服务验收：5 个进程、窗口标题 `AI Network Lab 1.3.3`、`/` 与 5 个 API 全部 200

---

# 上一版发行说明 v1.3.3

> AI Network Lab —— 智能网络科研工作台
> 当前包版本：v1.3.3（对应 `package.json`）

**这一版修「点了更新就闪退、版本还是旧的」。**

---

## 一、问题：下载成功了，安装却从来没成过

现象是：点「立即重启并安装」→ 应用窗口消失 → 重新打开还是旧版本；有时还伴随界面显示异常。

先把「下载」排除掉 —— 更新缓存里躺着的安装包是这样的：

| 项 | 值 |
|---|---|
| 缓存位置 | `%LOCALAPPDATA%\ai-network-lab-updater\pending\` |
| 文件 | `AI-Network-Lab-Setup-1.3.2.exe` |
| 字节数 | 125,009,264（与 Release 上的资产**逐字节一致**） |
| 校验 | `update-info.json` 中的 `sha512` 与下载时记录的一致 |

也就是说：**下载全程没问题，问题全在安装那一步。**

## 二、两个代码缺陷

**1. 安装器装完不会把应用拉回来。**

本项目用的是 assisted 安装器（`nsis.oneClick: false`）。electron-builder 的 NSIS 模板里，
这种安装器执行「装完自动启动应用」的条件是：

```nsis
${if} ${isForceRun}
${andIf} ${Silent}      ; ← 只有静默安装才拉得起来
```

而原来的代码调的是无参 `quitAndInstall()`，等价于 `isSilent = false` ——
**于是安装完应用不回来**，用户看到的就是「点一下更新，应用闪退，还得自己开」。
现在改为 `quitAndInstall(true, true)`，等价命令行 `--updated /S --force-run`。

**2. 「点完更新，界面整体显示异常」—— 我们在确认安装会启动之前，就把内部服务杀了。**

原逻辑是：先杀掉内部 Next 服务 → 再调 `quitAndInstall`。
但 `quitAndInstall` 是「立即返回、**随后**才去拉起安装器」，而它有可能失败
（更新缓存被清、安装器被安全软件拦下、spawn 报 EACCES 等）。

一旦失败，应用**不会退出** —— 可内部服务已经被杀掉了，界面上所有接口一起失败，
看起来就是整个界面坏掉。现在改为让服务跟随正常退出流程一起收：
`app.quit()` 紧随 `install()` 之后，释放 `resources/app` 句柄的时机依然早于安装器动手。

另外加了一道守护：`quitAndInstall` 只在 `install()` 成功时才会真的退出应用，
所以 5 秒后复查一次 —— 应用还活着就明确告诉你「安装没启动」以及下一步怎么办，
而不是让你对着一个点了没反应的窗口猜。

## 三、还有一个环境约束（这条最重要，请看一眼）

安装器不是直接往旧目录里覆盖文件，而是先把 `$INSTDIR` 整个**改名**到临时目录
（`un.atomicRMDir`），失败就还原并中止安装。

**Windows 不允许改名一个「内部还有文件被别的进程打开」的目录。**
只要有任何程序占着安装目录，这一步就会被挡下 —— 现场表现就是：

- 安装器窗口一直停着不动（CPU 几乎不涨），进程长期不退出
- 更新缓存里的安装包**永远不会被消费掉**
- 应用下次启动直接崩在 `Error loading V8 startup snapshot file`（目录被移走/半还原）

⚠️ **不要把安装目录当工作目录 / 项目目录用。** 把
`%LOCALAPPDATA%\Programs\AI Network Lab` 当作工作区的编辑器、知识库工具、
同步盘、索引服务，都会在更新时挡住安装器。安装前请把它们关掉。

> 卡住了怎么办：直接结束那个 `AI-Network-Lab-Setup-*.exe` 进程即可。
> 它握着安装互斥量，不结束的话之后**每次**安装都会立刻被中止、而且没有任何提示。

顺带说一句：NSIS 覆盖安装会先跑旧卸载器清空安装目录，所以放在安装目录里的
`.workbuddy/` 之类数据**会被一起删掉**，重要数据不要放在安装目录下。

## 四、这一版的其它改动

- **更新链路第一次有了落盘日志**：`%APPDATA%\ai-network-lab\logs\update.log`，
  记录版本、检查结果、**安装包真实落盘路径**、`quitAndInstall` 收到的参数。
  这次排查时应用侧一行痕迹都没有，全靠一个 178 字节的 `debug.log` 和缓存残留倒推
- 下载 / 安装阶段的失败现在会回传到界面（后台静默检查失败仍然不会打扰你）
- 状态行不再自相矛盾：以前「已下载完成」的卡片里可能还写着「点『下载更新』」
- 更新卡片底部新增**手动兜底入口**，直达发布页

## 五、怎么装

安装包是 `AI-Network-Lab-Setup-1.3.3.exe`。

本轮在这台机器上已经清掉了一个卡死约 15 分钟的 1.3.2 安装器进程，安装目录完好、
当前版本仍是 v1.2.4。**装 1.3.3 之前，请先关掉正在把安装目录当工作目录用的程序**，
否则会重复遇到同一处卡顿。

---

# 上一版发行说明 v1.3.2

> AI Network Lab —— 智能网络科研工作台
> 当时包版本：v1.3.2（对应 `package.json`）

**这一版只干一件事：让安装包真的出现在 Release 里。**

---

## 一、问题：连续两次「亮着绿灯失败」

v1.3.0 和 v1.3.1 的 Release 里都**只有一个 `.exe.blockmap`** —— 既没有 `.exe` 安装包，
也没有 `latest.yml`。而发布流水线**每一步都是绿的**，同一个 run 的备份工件有 119 MB，
说明安装包确实构建出来了，只是没能进 Release。

后果是静默的：点下载什么也拿不到；客户端「检查更新」永远失败（`latest.yml` 是
electron-updater 判断有没有新版本的**唯一依据**）。

## 二、根因：上传环节会「静默放弃」

查到了源码层面的原因 —— electron-publish 的上传路径有多处**只警告、不报错**的放弃分支：

- 目标 Release 已存在、且发布时间超过 **2 小时**时，`getOrCreateRelease()` 返回 `null`
  （除非设 `EP_GH_IGNORE_TIME=1`），随后 `doUpload()` 只打一条
  `log.warn("skipped publishing")` 就返回 —— **进程照样 exit 0**
- 构建过程中出错时，`app-builder-lib` 会 `cancelTasks()` 把上传任务丢掉，并把结果换成
  `Promise.resolve(null)`，同样不报错
- 任务被取消时 `awaitTasks()` 会直接 return，**连 `latest.yml` 都不会生成**

结论很直接：**这条路径不能用来判断「发布到底成没成」**，所以这一版不再依赖它。

## 三、怎么修的

1. **上传改由 `gh` CLI 显式完成。** `electron-builder` 现在只用 `--publish never` 负责构建
   （`publish` 配置段保留，`latest.yml` / `app-update.yml` 照常生成），上传交给
   `gh release upload --clobber` —— 失败必然非 0 退出，同一 tag 重跑也不会因为资产已存在而挂掉。
2. **上传前自检本地产物**：`release/*.exe` 与 `release/latest.yml` 缺任何一个就直接失败。
3. **上传后按字节数核对**：解析 `latest.yml` 里声明的 `path` / `size`，与 Release 上的实际资产
   逐一比对名字和字节数。这一步不仅能发现「全都没传」，也能发现「传了一半」。
4. **备份工件改为 `if-no-files-found: error`**，产物缺失不再只是一条警告。
5. 新增一个手动触发的 **Release cleanup** 流程，用来清掉那两次残缺记录（只删 Release、保留 tag）。

## 四、怎么装

**装这一版**，安装包是 `AI-Network-Lab-Setup-1.3.2.exe`。
v1.3.0 / v1.3.1 的 Release 里没有可下载的包，请忽略它们。

---

# 上一版发行说明 v1.3.1

> AI Network Lab —— 智能网络科研工作台
> 当时包版本：v1.3.1（对应 `package.json`）

**这一版修的是「发了但装不上」。v1.3.0 的 Release 里其实没有安装包。**

---

## 一、v1.3.0 的发布会「亮着绿灯失败」

先说结论：**v1.3.0 在 GitHub 上的 Release 是残缺的** —— 里面只有一个
`AI-Network-Lab-Setup-1.3.0.exe.blockmap`，**既没有 `.exe` 安装包，也没有 `latest.yml`**。

后果是静默的：

- 你去 Release 页面点下载，什么也拿不到
- 客户端「检查更新」永远不会成功 —— `latest.yml` 是 electron-updater 判断「有没有新版本」的**唯一依据**

而发布流水线**所有步骤都是绿的**。同一个 run 里的备份工件有 119 MB，说明安装包确实构建出来了，
只是没能进 Release。这就是最难查的一类故障：它不报错，只沉默。

## 二、修了什么

1. **安装包文件名去掉空格。** 文件名原先取自 `productName`（"AI Network Lab"），磁盘上是
   `AI Network Lab-Setup-1.3.0.exe`。但传到 Release 后资产名里的空格被规范化成了连字符，
   而 `latest.yml` 里记录的仍是带空格的原名 —— electron-updater 照着它去下载就会对不上号，
   卡在下载这一步。现在**磁盘文件名、Release 资产名、`latest.yml` 里的路径三者字面一致**。
2. **发布流程加了「发布后自检」。** 上传完之后会核对 Release 里是否**同时**有安装包和 `latest.yml`，
   缺任何一个就让发布任务**失败**。自检刻意放在备份工件之前 —— 万一失败，完整的产物仍然留档，
   可以手动补挂，不必重新构建。
3. **发布加了并发锁**，避免同一个版本被两次发布互相覆盖资产。

顺手修掉的还有两个 CI 失败（这些提交此前从未被 CI 跑过，一推就暴露了）：中文 PDF 导出的单测在
ubuntu 上因为没有中文字体而失败；数据库版本戳的单测在 Node 20 上因为 `node:sqlite` 不存在而崩。
两处都已修复。

## 三、怎么装

**已由 v1.3.2 取代。** v1.3.1 的 Release 同样没有安装包（只有一个 `.exe.blockmap`），请装 v1.3.2。

---

# 上一版发行说明 v1.3.0

> AI Network Lab —— 智能网络科研工作台
> 当时包版本：v1.3.0（对应 `package.json`）

**这一版把「研究规划」从一个只有界面的模块，补成了真正能用的模块。**

它此前是全项目唯一没接完数据层的功能：5 个标签页里只有 2 个能写，刷新即丢；里程碑存的是「第 12 周」这种**没有起点**的表示法，既答不出是哪个月，也判断不了「现在落后没有」。本次把五个方向全部补齐，标签页从 5 个增到 8 个。

---

## 一、周计划真的落库了，而且有了「项目起点」

- **周计划不再是内存态。** 「研究规划 → 周计划」原先是一段 `useState`，加/删/勾全在内存里，**刷新就没了** —— 这是全项目唯一一个「纯内存」的功能。现在有了真正的数据表与接口：按周隔离、可跨周移动、勾选完成会持久化。
- **新增「项目起始日」配置**（在研究规划里直接设置）。这是整个模块的关键：甘特图的里程碑存的是**周序号**（`"0"`..`"39"`，从早期脚本集直译来的表示法），没有起始日就没有锚点。设置之后：
  - 甘特图横轴显示**真实日期区间**（如 `01-05 ~ 02-01`），并画出「当前周」游标
  - 本周可用工时按配置算（默认 44 小时/周），不再是写死的数字
  - 未设置起始日时如实退回只显示周序号 —— 不假装知道日期
- **顺手修掉一个长期错位**：甘特图原先按字符串排序周序号，`"10"` 会排在 `"2"` 前面。现在按数值排，40 行的顺序才正确。
- 进度写入口统一成 `ProgressStepper`（±5% 步进）：Gantt、写作时间线、投稿时间表都能直接改进度 —— 原先写作时间线**没有任何写入口**，导致「总工时完成度」永远是 0%。

## 二、里程碑可以挂到实验/稿件上，进度跟着走

里程碑现在能关联一个**实验**或**稿件**，并据此派生进度：

- 实验：计划 → 0%，运行中 → 60%，完成 → 100%，**失败 → 0%**（失败要人来判断，程序不替你宣布完成）
- 稿件：已投稿 → 100%；草稿按字数比例推进，但**封顶 95%** —— 没投出去就不显示「完成」，避免「初稿写完 = 论文完成」的假象

两条**刻意保守**的规则，防止自动同步变成数据事故：

1. **默认关闭。** 没打开时，同步只告诉你「建议推进到 X%」，**绝不写入** —— 你手工填的进度不会被一个关联悄悄改掉。
2. **只推进、不回退。** 实验被改回「运行中」，也不会把里程碑从 100% 拉下来。

同步是一个**显式按钮**，不是隐藏行为：每次推进都会告诉你「改了哪几条、从多少到多少」。甘特图里还能看到「⇄ 实验：xxx」的关联标签。

另外，通知栏新增**计划落后提醒**：按项目起始日把周序号换算成真实日期，逾期的里程碑给高优先级提醒、14 天内到期的给低优先级。**拿不到起始日就整段跳过** —— 宁可少一条提醒，也不按错误的日期催你。

## 三、新增「计划 vs 实际」偏差复盘

新增「实际完成日」字段与一个专门的标签页，回答「计划的东西到底按时完成了没有」：

- 五种状态：提前完成 / 按时完成 / 落后 / 进行中 / **无法判定**
- **已完成但没填实际完成日的，一律记为「无法判定」** —— 不假装它按时
- 汇总给出各状态计数、平均偏差天数，以及**「最需要处理的」前 3 条**（排序为「落后优先」）
- 可一键**导出 Markdown 报告**，直接贴进组会材料或交给导师

页面上看到的和导出的报告**共用同一个计算函数** —— 不会两边各算各的。

## 四、AI 规划助手（用你自己的 API Key）

新增「AI 助手」标签页，三种模式：

- **生成本周计划**：把项目当前状态（里程碑进度、计划/实际的偏差、本周已有任务）交给模型，产出可执行的任务清单
- **进度风险体检**：基于真实偏差数据做评审，指出该优先处理的风险
- **里程碑拆解**：把某个里程碑拆成有先后顺序、能逐步交付的小任务

几个刻意的设计：

- **生成结果不会自动写库。** 点「生成」只展示，你核对后点「写入本周计划」才落库 —— **写进去的就是你屏幕上看的那几条**。否则连点两次「生成」会得到两份不同结果，而库里已经躺了上一份，没人能解释那些任务从哪来。
- **本周可用工时是硬约束**，会明确写进提示词并展示在结果里（不写这条，模型会热情地排出一个 60 小时的一周）。
- **模型不听话也不会白跑**：套了代码围栏、前后加一段废话、甚至给个 `hours: 999`，这里都会容错解析并把越界值截断；实在解析不出来，会把**模型的原始回复**摊给你看，而不是只甩一句「失败」。
- **没配 Key 时给的是可行动的错误**：「去设置里填 API Key」的按钮，而不是一个 400。

它复用应用内已配置的 LLM 网关，所以 GLM / DeepSeek / OpenAI / Ollama 等任何 OpenAI 兼容接口都能用，**Key 仍然只存在你本机**。

## 五、日历集成：把研究计划导进手机日历

新增「日历」标签页 + `.ics` 导出，可直接导入 **Google 日历 / Apple 日历 / Outlook**：

- 月历视图（**以周一为一周之始**，与周计划口径一致），点某天看当天有哪些任务与里程碑
- 一键导出 `.ics`：里程碑与周计划任务都会变成全天事件，并在**结束前一天**提醒（对里程碑来说「截止前一天」才有用）
- **重复导入是更新而不是堆重复项**（UID 由稳定 id 派生）
- 未设置项目起始日时，换算不出日期的甘特里程碑**直接跳过** —— 宁可少一条，也不塞一个错误的日期进你的日历

选了 ICS 而不是对接某个日历 API，理由很实际：它**不需要账号、不需要授权、不发任何网络请求**，导出完就是一个文件。

## 六、修复

- **编辑里程碑不再莫名 500**：旧实现把请求体直接透传给数据库，前端多传一个界面用的字段就报错。现在非法字段被忽略，进度值夹在 0–100，不存在的 id 返回 404。
- **不允许写入「半截关联」**：关联类型与关联对象必须成对，只给一半的整对置空 —— 否则同步会扫到一批永远解析不出对象的行，白白多查一次。

---

## 验证到什么程度（如实说明）

- `tsc --noEmit` 0 错、`eslint .` 0 错
- **单测 269/269**（19 个文件；本轮为研究规划新增 4 个测试文件，共 116 例）
- **接口端到端 156/156**：拿打包模板复制一份临时库 → 真起服务 → 真打 HTTP 接口 → **最后直接读 SQLite 文件**核对确实落库。AI 助手这条链路在测试里起了一个**本地假 LLM 服务**并正规配置进去，因此是真发请求、真解析响应，而不是把调用打桩。
- **未做**：浏览器级 UI 冒烟（本机未安装 Playwright 浏览器）。前端消费的字段名已与实测的接口响应逐项核对一致。

安装包由打 tag 后的 CI 构建产出。

---

# 上一版发行说明 v1.2.4

> AI Network Lab —— 智能网络科研工作台
> 当时包版本：v1.2.4（对应 `package.json`）

**这一版把「检查更新」真正打通、换掉了默认图标，并给「多版本并发」装上了闸门。核心是修掉自动更新长期不生效的真因。**

---

## 一、自动更新为什么一直不生效（真因）

先给结论：**不是网络问题，也不是你机器的问题 —— 是发布侧的配置少写了一个键。**

`electron-builder.yml` 的 `publish` 段没有指定 `releaseType`。这个选项在 electron-builder 里**默认是 `draft`（草稿）**。
也就是说，CI 每次辛苦打完包，上传上去的都是一个**草稿 release**。而草稿在 GitHub 上：

- 不算「latest」——`https://github.com/<owner>/<repo>/releases/latest` 不再指向任何 tag（实测直接 302 到 releases 列表页）
- 匿名用户看不到

于是客户端这边：

1. electron-updater 先去取「最新 tag」，请求 `releases/latest` 并带 `Accept: application/json`
2. 跟着跳转走，GitHub 回了一个 **406**
3. 报错 `Cannot parse releases feed: ... HttpError: 406`
4. **而这句错误被 `console.warn` 悄悄吞掉了**

所以你在界面上看到的「检查更新毫无反应」，其实是**每次启动都在失败**，只是没人告诉你。

**已修**：显式写入 `releaseType: release`。

> ⚠️ **还需要你在 GitHub 上补一步**：历史上已经产生的那些 release 是草稿状态，需要手动改成正式发布；
> 或者重新打一个 tag 让 CI 覆盖。否则客户端依然找不到「latest」。

## 二、检查更新现在有界面了，而且不再静默失败

原来 `preload.js` 虽然暴露了检查更新的接口，但**前端一行都没调用过** —— 等于没有入口。现在：

- **设置页新增「软件更新」卡片**：显示当前版本、手动检查按钮，并如实告诉你结果
- **失败会翻译成人话**。同样一个失败，原来只给你一句 `HttpError: 406`，现在会说：
  「更新源暂时没有可用的正式发布（GitHub Releases）」——并附上「这是发布侧的问题，不是本机故障」
  - 出错分类：无正式发布 / 缺 `latest.yml` / 网络不可用 / 开发模式 / 更新组件缺失
- **有新版会三重提醒**：系统对话框 + 界面顶部常驻横幅（可忽略）+ 消息提示
  - 之所以不只靠对话框：用户点一次「稍后」对话框就消失了，等于提醒过了、但没记住。横幅只要没处理就一直挂着，并带「下载更新」按钮；下载完再给「立即重启并安装」

## 三、只能开一个客户端 —— 而且只允许存在一个版本

### 3.1 同一份安装的第二个实例（原有能力，这次堵上竞态）

单实例锁原本就在，实测也是生效的（双开时第二个实例会自动退出，关窗后进程全部归零、无残留）。
但里面有一个**退出竞态**值得堵上：原先拿不到锁只调 `app.quit()`，而 quit 是「请求退出」，
要等 `before-quit` 走完才真正结束，`app.whenReady()` 在这个窗口期仍可能被触发 ——
那样第二个实例会照样跑完启动流程、拉起第二份内部服务（多一批子进程 + 第二个 SQLite 句柄，
两个实例同时写同一个 db 文件是有风险的）。

现在改为 `app.exit(0)` 立即终止，并给启动流程加了短路兜底。

### 3.2 不同版本之间（这次新增的闸门）

单实例锁有个前提容易被忽略：**它只在「双方都调用它」时才互斥**。
而**自动更新用的那个锁是 v1.2.1 才引入的，v1.2.0 及更早的版本里根本没有这段代码**，
历史安装包也改不了。它们和新版**共用同一个数据目录、同一个数据库文件**
（`%APPDATA%\ai-network-lab\db\custom.db`）—— 只要新旧两个版本同时开着，
就是两个进程并发写同一个库。这就是「用多版本开并发」的口子。

这次从三层把它关掉：

**① 启动时主动查一遍系统（多层闸门）**
新版启动时会枚举同名进程。只要发现有**另一个安装路径**的同名客户端在跑，
立刻弹框告诉你「哪个 PID、在哪条路径」，并且**直接退出**，连解压和建库都不做。

- 判定只看「exe 绝对路径不同」：同路径的第二个实例仍交给单实例锁处理。
  这样升级后自动重启时，旧进程还没退干净也不会把自己卡死在启动对话框上
- 进程鉴别靠命令行特征：排除带 `--type=` 的渲染/GPU 子进程，以及跑 `server.js` 的内部服务子进程
  （本应用是用同一个 exe 以 Node 身份跑内部服务），避免把自己刚拉起的子进程误判成「第二个客户端」
- 查不出来就放行（只记日志），不会因为安全软件挡了进程查询就把你锁在自己的应用外面

> 这里踩了一个很隐蔽的坑，值得说明：**WMI 的进程查询会偶发返回空结果**。
> 实测在一台机器上连续采样 90 秒，进程明明一直在跑，却有两次查到「0 个进程」——
> 而「查不到」在这个问题上恰好等价于「没有冲突」，于是守卫**不报错、不弹窗，就是拦不住**，
> 属于最坏的一类失效（静默）。现在的结果会再用 `tasklist`（走另一条内核快照路径、不经过 WMI）
> **交叉确认**，并且**最多重试 3 次**；只有两边都说「没有」才敢相信。

**② 反向场景：新版开着，你又去点了旧版图标**
旧版内部没有守卫代码，拦不住它自己启动 —— 能做的是**尽快让你知道**。
窗口重新获得焦点时会做一次复检（60 秒节流），发现后弹系统提醒 + 界面横幅，让你关掉它。

**③ 从安装器层面保证磁盘上只有一份**
安装选项 `allowToChangeInstallationDirectory` 已改为 `false`（原先可改）。原因很直接：
允许改目录，你就能把新版装到另一个目录，磁盘上于是同时存在两份不同版本 —— 这正是上面那个口子的入口。
固定后升级会直接覆盖旧版本（NSIS 检出已安装版本时会沿用注册表里记录的目录），**磁盘上永远只有一份**。

> 顺带说明「同一路径但装的是旧版本」这一种：它不归启动守卫管（守卫只看路径是否不同），
> 而是**安装器在覆盖文件之前就解决了**。electron-builder 的 NSIS 会按
> `$_.Path.StartsWith('$INSTDIR')` 找出正在运行的同目录进程，弹「应用正在运行」并结束它 ——
> 否则运行中的 exe 根本覆盖不了。所以「同路径不同代码」也不可能并存。

**④ 数据层面的兜底：不允许旧程序打开新数据**
本机会把程序版本写进 SQLite 文件头自带的 `PRAGMA user_version`（不建表，Prisma 也看不见它）。
启动时如果发现「**库里记录的版本比当前程序新**」，说明这份数据已经被更新的版本写过，
旧代码不认识新增的列和表，继续启动就可能把新数据悄悄写坏 —— 所以直接拒绝启动，并保留全部数据。
反方向（库比程序旧）永远放行，正常升级不受影响。

> **一件需要你做的事**：本次没有在本机找到任何 `1.2.0` 的安装包文件（全盘搜过，只有 git 标签、
> 桌面快捷方式，以及 `%LOCALAPPDATA%\Programs\AI Network Lab` 里已安装的那一份 1.2.0）。
> 请**安装这一版 1.2.4 把它原地覆盖掉** —— 它是唯一一个没有单实例锁的版本，
> 覆盖之后就再没有「新旧并存」的可能了。历史 Release（v1.2.0 / v1.2.1 / v1.2.2）目前都是 GitHub 草稿，
> 建议在 GitHub 上一并删除草稿，避免以后被误下载。

> **验证到什么程度（如实说明）**：单测 152/152、类型与 lint 全绿、安装包内的壳层脚本与仓库源码
> **逐字节一致**（sha256）、前端产物也确认带上了本次全部改动。但**「两个版本同时运行会互相拒绝」
> 这条实机演出没有跑成** —— 开发沙箱不允许启动被测程序、也不允许它 spawn PowerShell/taskkill，
> 守卫在那种环境下只能走「查不到就放行」的分支。该场景的判定逻辑由 19 条单测覆盖，
> 你可以在自己的桌面会话里跑 `node .recon/guard-live-test2.mjs` 复核。

## 四、应用图标

打包日志里那句 `default Electron icon is used` 没了。图标由 `node scripts/make-icon.mjs` 生成：

- `build/icon.ico` 内含 **16/24/32/48/64/128/256 七个尺寸**，Windows 按 DPI 和各显示位置挑最近的那张
- 图案是「中心枢纽 + 四个卫星节点」，深蓝到青的渐变

这里有个实测出来的细节：**同一个图案在小尺寸下会失效**。带连线的完整版缩到 16px 时，
四条连线会和节点糊成一个「十字」，看着像医疗标志而不是网络拓扑。
所以 ≤32px 单独用了一套**去掉连线、放大节点**的简化几何 —— 16px 下能清楚认出是「一个枢纽 + 四个节点」。

---

# 上一版发行说明 v1.2.3

> AI Network Lab —— 智能网络科研工作台
> 当时包版本：v1.2.3（对应 `package.json`）

**本次三件大事：删掉一个实测不可用的功能，把笔记导出真正接进 Obsidian，再把论文写作串成闭环。**

---

## 一、移除「智能组网实验室」（INET）

实测结论：这个功能**跑不起来**，而且有两道互相独立的门槛。

**门槛 A —— 前端从来没有 manifest 配置入口。**
点击「运行场景」时只发送 `scenarioType` 与 `parameters`，没有 manifest；全前端搜索 `manifest` 在组件层**零命中**。
服务端拿到空的 `projectRoot`，`validateManifest` 必然返回诊断 → 任务在到达运行器之前就被判为 failed。
也就是说：**即使你自己把 OMNeT++/INET 装好，也无法从界面跑起来**。

**门槛 B —— 依赖外部工具链。**
它需要 OMNeT++ + INET，这是要自行编译、数 GB 量级的 C++ 工具链。

让使用者为此自建一套重型环境，成本远大于收益，因此**整体移除**：相关 UI、API、库、测试、文档、Prisma 模型全部删除。
老版本升级时，`migrate-database.js` 会把这些功能遗留的空表清理掉（先子表后父表，避免外键阻挡），**你的文献与笔记数据不受影响**。

**组网仿真能力并没有丢**：内置引擎的「组网仿真实验」不需要任何外部依赖，同参数同种子结果完全可复现，是真正可用的那一个。

## 二、导出 Markdown 直接写入 Obsidian vault

以前只能「另存为」得到一份 Obsidian 兼容的 md，你还得手动搬进 vault —— Obsidian 无从索引、无从双链。
现在：

- 设置页新增「**Obsidian 笔记库**」：vault 目录（系统原生目录选择器）+ vault 内子目录 + 开关
- 导出菜单新增「**同步这一篇到 vault**」「**全部文献笔记同步到 vault**」
- 「导出 Markdown」在 vault 可用时**顺便写入**（vault 写入失败不影响下载）
- 导出的 md 带 YAML frontmatter 与 `#标签`，Obsidian 打开即纳入索引

写入路径做了围栏校验：绝对路径 / `..` / 盘符 / UNC 一律拒绝或净化；写入前用 `path.resolve` 二次确认仍在 vault 内。
端到端实测确认：**vault 之外的目录不会被写入**。同名笔记自动追加短 id 后缀，批量同步不会互相覆盖。

## 三、论文写作终于「闭环」了

以前「论文写作」有结构检查、句式、审稿回复、投稿清单，但**没有地方真正写正文**——你在别处写，引用编号、参考文献、格式全靠手工对齐。
现在「论文写作」的第一个标签页是「**写作工作台**」，把链路一次接通：

**文献库 → 插入引用 → 写章节 → 引用自动编号 → 生成参考文献 → 导出**

- **新建即开工**：自动带上 Abstract / Introduction / Related Work / System Model / Method / Experiments / Conclusion 骨架，不用对着空白页发呆
- **写的时候不用管编号**：在右侧搜文献库，点一下就把 `[@paperId]` 插到**光标处**（光标位置会被还原，不用再找刚才写到哪）
- **编号全自动**：按正文**首次出现顺序**编号，删掉一段或调整章节顺序后会重排
- **预览 = 导出**：编号只有一个来源（服务端），所以你右边看到的参考文献，和导出文件里的**是同一份**，不会出现「预览 [3] 导出变 [5]」这种对不上的情况
- **引用错了会告诉你**：`[@id]` 指向的文献在库里不存在时，它**仍然占号**并在面板和导出里标红，而不是悄悄消失
- **不怕丢稿**：停笔 800 ms 自动保存，`Ctrl/Cmd+S` 立即保存；保存失败会保留你刚写的内容并可点重试；切走页面前会把没落盘的改动补写
- **导出 md / txt**：md 带标题层级与规范化的 IEEE 风格参考文献；中文文件名不乱码

字数统计按中英混排分别计数（中文按字、英文按词），并自动忽略 `[@..]` 标记、代码块和链接。

## 四、顺带修掉的两个隐患

1. **INET 运行器一旦启动失败，会把整个后端打崩。** 子进程的 `'error'` 事件没人监听，而 Node 在「可执行文件不存在」时只发 `'error'`、不再发 `'close'`——EventEmitter 无监听者就会抛未捕获异常，进程直接退出。这条调用又在 fire-and-forget 的异步函数里，等于**一次失败的实验让整个内部服务消失**，你看到的是界面突然全废。
2. **测试跑完不退出，会让发布流水线永远卡住。** `vitest run` 在本项目里用例全绿后进程不退出（既有问题，改动前的基线同样复现）。这会让发布的 `Unit tests` 步骤一直挂到 GitHub 默认的 **6 小时**上限，安装包永远发不出来。已改用 `pool: 'threads'` 并给发布任务加超时兜底。

## 五、打包配置里一个会让发版彻底失败的错键（本次才发现）

`electron-builder.yml` 里限制 Chromium 语言包的键写成了 `win.locales`。这个键**不存在**——electron-builder 26 里它叫 `electronLanguages`。

后果比"少了一行配置"严重得多：整段 `win` 配置会被 JSON schema 校验拒绝，打包**当场终止**，而报错只有一句

```
configuration.win should be one of these: null
```

**它不告诉你是哪个字段错了。** 于是 CI 的发布任务会以同样原因失败——**也就是说 v1.2.2 的 Release 从来没有产出过可用安装包**，而失败原因藏在这么一句看不出所以然的话里。

本次已改为 `win.electronLanguages`，并**在本机完整跑通了 `desktop:build` → `electron-builder --win` 全流程**，确认能产出安装包。

---

# 上一版发行说明 v1.2.1

> AI Network Lab —— 智能网络科研工作台
> 当时包版本：v1.2.1（对应 `package.json`）
> 累积自上一个可用版 v1.1.0（桌面安装包 v1.1.0）

本版是一次**累积功能 + 打包可靠性修复**的发布。相对 v1.1.0，新增了「Zotero → 本软件 → Obsidian」科研工作流的补齐，并集中修复了桌面端解压/升级链路的隐患，同时把安装包与运行时体积砍掉约七成。

**强烈建议升级，尤其是桌面版用户。**

---

## ⚠️ 本次最需要关注的修复

### 1. 解压逻辑由"先删后解压"改为 fail-safe 原子替换

**风险（旧实现）**：`ensureAppExtracted()` 先删除旧的 `resources/app` 目录，再解压新 `app.zip`。一旦解压中途失败（磁盘满、被杀进程、压缩包损坏），旧目录已删、新目录不全——**应用直接打不开**，且无法自愈。

**修复**：解压到临时目录 `app.new` → 校验通过 → 原子改名替换，并写入 `.extract-ok` 完整性标记。任一步失败都保留原目录，安装始终处于可用状态。

### 2. 安装包与运行时体积大幅下降

| 项目 | 修复前 | 修复后 |
| --- | --- | --- |
| Next standalone 产物 | 607 MB | **92.9 MB** |
| `resources/app.zip` | 87 MB | **31.2 MB** |
| Chromium 语言包 | 55 个（约 48 MB） | 2 个（zh-CN / en-US） |
| 构建期误拷 `test-results` | 366 MB | 已挡在 tracing 之外 |

- `prepare-standalone.js` 由黑名单改为**白名单裁剪**：顶层只保留运行必需项，递归清除 `*.tmp*`、非 sqlite 的 `wasm-base64`、`*.map`、`*.d.ts`。
- `next.config.ts` 的 `outputFileTracingExcludes` 补挡 `test-results` / `playwright-report` / `e2e` / `tests` / `docs` / `.github` / `db` / `*.db`。

---

## v1.2.0 新增功能（本版一并包含）

### 补齐「Zotero → 本软件 → Obsidian」科研工作流
导入/同步**不会**自动调用 LLM。

- **论文阅读笔记导出 Markdown / PDF / TXT**：论文详情页「导出笔记」走 `/api/papers/:id/notes`
- **科研笔记服务端导出 MD/PDF**：`/api/notes/export/:id`，桌面端不再依赖前端 Blob 拼文件
- **论文库 CSV 是论文列表**：UTF-8 BOM + 题名/作者/期刊/年份/DOI/状态/标签
- **导入 RIS / BibTeX**：按 DOI / Zotero key / 标题去重合并
- **导入 PDF 入库**：PDF 落到本机 `library/pdfs/`，可挂到已有论文或新建条目；不解析、不送 LLM
- **Zotero Web API 同步**：设置页填写 User ID + API Key，一键拉条目元数据

### 其他
- 桌面端升级时对 `Paper.doi` / `zoteroKey` / `pdfPath` 做无损 `ALTER TABLE`

---

## 工程卫生

- 移除已完全合并的 `feature/inet-lab` 工作树与分支
- 归档并移除两个早期废弃仓库（初版四 demo 之一的 `AI-net`、更早的 `智谱AI` 分支）

---

## 安装

### 桌面版（推荐）
```bash
npm run desktop:dist        # 重打安装包 → release/AI Network Lab-Setup-1.2.1.exe
```
安装后直接使用；升级时研究数据（`%APPDATA%\ai-network-lab\db\custom.db`）自动保留，旧代码会在启动时按 `BUILD_ID` 对比被安全替换。

### Web 版
```bash
npm install
npm run db:push             # 初始化数据库（含新字段）
npm run dev                 # http://localhost:3000
```

## 使用说明
应用内「使用说明」功能区覆盖完整上手指引（快速开始 / 接入 AI / API 速查 / 数据备份 / FAQ），也可从新手引导直达。
